---

---

<section>
    <div class="card">
        <div class="card-img">
            <img src="" alt="">
        </div>
        <p>Nombre</p>
    </div>
</section>

<style>
    section {
        margin-top: 6rem;
        display: flex;
        justify-content: center;
        padding: 2rem;
    }

    .card {
        width: 300px;
        height: 350px;
        background-color: #a64472;
        border-radius: 1rem;
        background: rgba(41, 0, 19, 0.35);
        box-shadow: 0 2px 16px 0 rgba(44, 0, 23, 0.15);
        backdrop-filter: blur(12px) saturate(160%);
        -webkit-backdrop-filter: blur(12px) saturate(160%);
         border-bottom: 1.5px solid rgba(33, 0, 20, 0.08);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;

    .card-img {
        width: 90%;
        height: 70%;
        background-color: #fdfdfb;
        border-radius: 1rem 1rem 1rem 1rem;
        overflow: hidden;
    }
    p{
        margin: 1rem 0;
        font-size: 1.2rem;
        color: #fff9db;
    }
</style>